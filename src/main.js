const app = document.getElementById('app')
const p = document.createElement('p')
p.textContent = 'Built with Vite — open locally with `npm run dev` or build with `npm run build`.'
app.appendChild(p)

// Example of loading an image from assets
// If you add an image at assets/images/example.png you can uncomment below to show it
// const img = document.createElement('img')
// img.src = '/assets/images/example.png'
// img.alt = 'example'
// app.appendChild(img)
